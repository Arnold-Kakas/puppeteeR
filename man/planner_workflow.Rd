% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{planner_workflow}
\alias{planner_workflow}
\title{Build a planner workflow}
\usage{
planner_workflow(
  planner,
  workers,
  evaluator = NULL,
  max_replans = 2L,
  max_steps = 10L,
  parse_plan = NULL,
  state_schema = NULL
)
}
\arguments{
\item{planner}{An \code{Agent} object used for planning (typically a high-capability
model such as Opus).}

\item{workers}{Named list of \code{Agent} objects that execute plan steps.}

\item{evaluator}{An optional \code{Agent} object that reviews completed results.
If \code{NULL}, the workflow ends when the plan is exhausted.}

\item{max_replans}{Integer. Maximum number of replanning rounds (default 2L).}

\item{max_steps}{Integer. Expected maximum plan length, used to size the
termination guard (default 10L).}

\item{parse_plan}{Optional \verb{function(text)} that converts the planner's raw
response into a list of \code{list(worker, instruction)} items. If \code{NULL}, a
default line-by-line parser expecting \code{worker_name: instruction} is used.}

\item{state_schema}{A \link{WorkflowState} or \code{NULL} (uses default).}
}
\value{
A compiled \link{GraphRunner}.
}
\description{
A high-tier planner agent creates a step-by-step plan; a pure-R dispatcher
routes each step to the appropriate worker agent (no LLM call per dispatch);
an optional evaluator agent decides whether the results are complete or
require replanning.
}
\details{
Graph: \verb{START -> planner -> dispatcher -> [workers] -> dispatcher -> ... -> evaluator -> (done -> END | replan -> planner)}.

The planner must respond with one step per line in the format
\code{worker_name: instruction}. A custom \code{parse_plan} function can be supplied
to handle alternative formats.
}
\examples{
\dontrun{
runner <- planner_workflow(
  planner   = agent("planner",    ellmer::chat_anthropic(model = "claude-opus-4-6"),
                    instructions = "Break tasks into steps for 'researcher' and 'writer'."),
  workers   = list(
    researcher = agent("researcher", ellmer::chat_anthropic(model = "claude-haiku-4-5-20251001")),
    writer     = agent("writer",     ellmer::chat_anthropic(model = "claude-haiku-4-5-20251001"))
  ),
  evaluator = agent("evaluator",  ellmer::chat_anthropic(model = "claude-opus-4-6"))
)
result <- runner$invoke(list(messages = list("Write a short report on tidy data.")))
result$get("results")
}
}
