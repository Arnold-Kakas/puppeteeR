% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{retry_policy}
\alias{retry_policy}
\title{Define a retry policy for a graph node}
\usage{
retry_policy(max_attempts = 3L, wait_seconds = 1L, backoff = 1)
}
\arguments{
\item{max_attempts}{Positive integer. Total number of attempts (including the
first). Must be >= 2.}

\item{wait_seconds}{Non-negative number. Seconds to wait between attempts.
Each subsequent wait is multiplied by \code{backoff} (default 1 = no backoff).}

\item{backoff}{Positive number. Multiplier applied to \code{wait_seconds} after
each failed attempt. Use \code{2} for exponential backoff.}
}
\value{
An S3 object of class \code{retry_policy}.
}
\description{
Passed to \code{\link[=add_node]{add_node()}} via the \code{retry} argument. When a node function throws
an error, the runner waits \code{wait_seconds} and retries up to \code{max_attempts}
times before re-throwing.
}
\examples{
rp <- retry_policy(max_attempts = 3L, wait_seconds = 1L, backoff = 2)
}
