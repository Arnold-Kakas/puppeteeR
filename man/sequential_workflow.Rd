% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{sequential_workflow}
\alias{sequential_workflow}
\title{Build a sequential workflow}
\usage{
sequential_workflow(agents, state_schema = NULL)
}
\arguments{
\item{agents}{Named list of \code{Agent} objects, executed in order.}

\item{state_schema}{A \link{WorkflowState} or \code{NULL} (uses default
\code{messages} + \code{reducer_append()}).}
}
\value{
A compiled \link{GraphRunner}.
}
\description{
Creates a linear chain where each agent receives the last message and appends
its response: \code{agent1 -> agent2 -> ... -> END}.
}
\examples{
\dontrun{
runner <- sequential_workflow(list(
  writer   = agent("writer",   ellmer::chat_anthropic()),
  reviewer = agent("reviewer", ellmer::chat_anthropic())
))
result <- runner$invoke(list(messages = list("Write a haiku.")))
}
}
