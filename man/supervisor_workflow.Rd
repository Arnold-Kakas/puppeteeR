% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{supervisor_workflow}
\alias{supervisor_workflow}
\title{Build a supervisor workflow}
\usage{
supervisor_workflow(manager, workers, max_rounds = 10L, state_schema = NULL)
}
\arguments{
\item{manager}{An \code{Agent} object acting as the supervisor.}

\item{workers}{Named list of \code{Agent} objects.}

\item{max_rounds}{Integer. Maximum number of manager turns (default 10).}

\item{state_schema}{A \link{WorkflowState} or \code{NULL} (uses default).}
}
\value{
A compiled \link{GraphRunner}.
}
\description{
Creates a hub-and-spoke graph: a manager agent directs work to worker agents
one at a time. The manager's response is expected to contain the name of the
next worker (or \code{"DONE"} to stop).
}
\examples{
\dontrun{
runner <- supervisor_workflow(
  manager = agent("manager", ellmer::chat_anthropic(),
                  instructions = "Delegate to 'writer' or reply 'DONE'."),
  workers = list(writer = agent("writer", ellmer::chat_anthropic()))
)
result <- runner$invoke(list(messages = list("Write a short story.")))
}
}
