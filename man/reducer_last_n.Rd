% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{reducer_last_n}
\alias{reducer_last_n}
\title{Reducer: keep only the last \code{n} entries}
\usage{
reducer_last_n(n)
}
\arguments{
\item{n}{Positive integer. Maximum number of entries to retain.}
}
\value{
A two-argument function \verb{function(old, new)}.
}
\description{
Appends \code{new} to \code{old} then trims the list to at most \code{n} entries by
dropping the oldest. Use this instead of \code{\link[=reducer_append]{reducer_append()}} whenever the
channel feeds into an LLM call, to prevent the context window from growing
unboundedly and causing connection errors on long workflows.
}
\examples{
r <- reducer_last_n(3L)
r(list("a", "b", "c"), "d")   # drops "a", keeps "b", "c", "d"
}
